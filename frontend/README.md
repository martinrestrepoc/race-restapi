# Frontend

This directory is reserved for the React, TypeScript, and Vite application defined
in the project architecture.

The frontend has not been scaffolded yet. It will authenticate directly with
Keycloak using Authorization Code Flow with PKCE and consume the NestJS REST API;
it must never connect directly to PostgreSQL.
